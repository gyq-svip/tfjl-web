# 交接单｜2026-08-27 白屏 P0 事故处理

> 放置位置：`d:\tfjl-web\HANDOVER_2026-08-27_白屏事故交接单.md`
> 状态：白屏代码已修复并部署（线上 sw.js = s1.0.350），但【恢复验证 / 铁律收口】尚未完成。
> 接手人：换号后的新会话。请先读 `d:\tfjl-web\.codebuddy\memory\2026-08-27.md` 看全天详细过程。

---

## 一、事故根因（一句话）
SW 的 `install` 里 `clients.claim()` 强接管 + `fetch` 对 HTML/JS/CSS 用 `staleWhileRevalidate`（先返回旧缓存）。旧缓存里的 `app-core.js` 在某版本缺失 `const HB_JITTER` 声明 → 页面加载抛 `ReferenceError` → `app-core.js` 中断 → `_hideLoadingScreen` 永不执行 → 所有人（web + APP）白屏。

## 二、已完成的修复（线上已部署，验证通过）
文件层 `app-core.js` 本身完好（线上 25511 字节、含 `HB_JITTER`），问题只在 SW 缓存策略。
线上 `sw.js = s1.0.350` 包含三道修复：
1. `fetch` 对 HTML(navigate/document) 与 JS/CSS(script/style) 改 `networkFirst` + 8s 超时回退（不再返回损坏旧缓存）。
2. `activate` 删除【所有】runtime 缓存（含当前版本），强制新 SW 首次请求走网络。
3. 双 `navigate` 硬拉：① 新 SW `install` 分支 claim 后对受控客户端 `client.navigate(url)`；② 旧 SW `_pollLatestVersion` 轮询发现新版本时也对客户端 `client.navigate(url)`。白屏页收不到消息，唯有硬 navigate 能拉回。
# 关键部署教训：SW 修复 commit 必须【不带 [skip ci]】，否则 deploy.yml 不跑、线上不更新、用户永久白屏。本次 346 那次带 [skip ci] 导致线上卡在 345，后来 347/349 普通 commit 才触发部署到 350。

## 三、当前做到哪里（进度）
- ✅ 根因定位
- ✅ 代码修复（sw.js networkFirst + activate 清缓存 + 双 navigate）
- ✅ 推送 + 手动 bump + 部署到线上 350（CI 全量校验通过）
- ✅ 确认线上 app-core.js 完好、sw.js=350 含双 navigate
- ⏳ **恢复验证**（接手人需确认用户是否都起来）
- ⏳ **铁律收口**（见下“未完成”）

## 四、接手人（你换的号）现在要做什么
1. **确认恢复**：
   - 自己先验证：web 端 F12→Application→Service Workers→Unregister→Ctrl+Shift+R 硬刷，应立刻恢复；APP 端任务管理器杀 tfjl 进程→重开 APP 应立刻恢复。
   - 询问/观察其他白屏用户：旧 SW 每 ≤5 分钟轮询发现 350 → 自动 `navigate` 硬拉白屏页 → 恢复。web 端基本确定有效；APP（Tauri WebView2）退出重开一定好，自动 navigate 取决于 WebView2 支持，需实测确认。
   - 如仍有用户起不来，查看其诊断：`diag-<id>.json` 的 gist，看 heartbeatMin 等是否正常（老虎那条脏数据 fn=https 已被 526 行 filter 自愈，无需手改）。
2. **收口铁律（恢复完成后做）**：
   - 当前为救白屏，保留了 `install` 分支与 `_pollLatestVersion` 的 `skipWaiting` + `navigate` 强接管能力。恢复后需收紧：前台（页面可见）绝不自动升级/强推，仅在挂托盘/隐藏态才允许 SW 静默接管。
   - 具体：把 `forceReloadEnabled` 默认 false（已改）；`_maybeForceReload` 仅托盘态触发；`navigate` 硬拉应只在“紧急恢复白屏”场景保留，普通升级改回“弹气泡由用户确认”。
   - 铁律原文在 long-term memory / 项目约定里，收口前先调出确认措辞。

## 五、我（本会话）接下来若继续要做的
- 等接手人反馈“用户都恢复”后，执行铁律收口 PR（sw.js 前台不 skipWaiting、navigate 仅限紧急恢复）。
- 补一个保险：APP 启动时强制清一次 SW 缓存（防 WebView2 旧 SW 不自动 navigate），需评估是否必要。
- 把“SW 修复必须普通 commit 触发部署”写进 CI 或文档，避免再犯。

## 六、关键文件 / 位置速查
- 事故与修复全过程：`d:\tfjl-web\.codebuddy\memory\2026-08-27.md`
- 长期约定（铁律）：`d:\tfjl-web\.codebuddy\memory\MEMORY.md`（如没有，看 memory 里有无铁律条目，没有就问用户）
- SW 源码：`d:\tfjl-web\sw.js`（看 54 行 CACHE_VERSION、116-128 行 _pollLatestVersion、155-180 行 install 分支、fetch 的 networkFirst 段、activate 清缓存段）
- APP 加载地址：`d:\tfjl-web\src-tauri\times\tauri.conf.json` 第 17 行 url = `https://gyq-svip.github.io/tfjl-web/`（APP 与 web 同源同 SW）
- 部署工作流：`d:\tfjl-web\.github\workflows\deploy.yml`（push main 触发，会 bump CACHE_VERSION 并推回 main）
- 线上验证命令（需 token，token 在 MEMORY.md 或用户授权里）：
  `curl.exe -s "https://gyq-svip.github.io/tfjl-web/sw.js" | Select-String -Pattern "const CACHE_VERSION"`
  `curl.exe -s "https://gyq-svip.github.io/tfjl-web/app-core.js" | Select-String -Pattern "const HB_JITTER"`

## 七、风险 / 注意
- 不要再用 `[skip ci]` 推 SW 修复，否则线上不更新。
- 不要手动改线上 gist（老虎脏数据已代码自愈）。
- 铁律收口前，350 的 navigate 强拉仅在白屏恢复场景发挥作用，属紧急例外，不算违规升级。
