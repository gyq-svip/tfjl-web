# 工作日志

> 倒序排列（最新在上）。每条记录：时间 · 主题 · 涉及文件/commit · 测试结果 · 注意事项。

***

## 2026-08-31（单日 5 个发版：v2.0.23 → v2.0.26，26+ 提交）

### 23:00 · 收尾：工作日志与铁律归档

* 整理当日工作日志写入 [工作日志.md](file:///d:/tfjl-web/工作日志.md)。

* 将当日重要事项、易错点、常用规则追加进 [铁律.md](file:///d:/tfjl-web/铁律.md)（见新增章节「八、发布脚本与双远程同步」「九、波数监控 per-window 配置」「十、升级回退链三级」）。

### 11:14 · publish\_update.ps1 自愈推送（commit 93822c0）

* **背景**：打包期间 CI 自动提交（sw\.js CACHE\_VERSION + version.json 注入 frontVersion/deployTag）造成本地与远端分叉，`git push origin main` 被拒。

* **修复**：给 `publish_update.ps1` 加自愈链路：

  1. push 被拒 → `git stash` 暂存本地未提交改动
  2. `git pull --rebase origin main` 同步远端
  3. version.json 冲突**智能合并**：发布字段（version/pub\_date/download/signature）取本次发布值，frontVersion/deployTag 保留 CI 注入值
  4. `git rebase --continue` → 重推一次

* **双远程同步陷阱**：本地 rebase 重写哈希后，Gitee 上的旧哈希不在本地历史 → 后续每次 `push gitee` 都被拒，需一次性 `--force-with-lease` 强制同步（用户已确认该操作模式）。

* **测试**：v2.0.26 全链路验证通过——GitHub Pages updater.json/version.json 均 2.0.26，Gitee release v2.0.26 exe 4837252 字节直链 200 且大小一致，双远程同步至 93822c0。

### 11:09 · 发布 v2.0.26（commit 0acd7f7）

* updater.json/version.json 推 GitHub Pages，installer 仅传 Gitee release。

* 验证：GitHub Pages 线上索引已更新，Gitee 直链 200 + 字节数一致。

### 11:02 · release v2.0.26 三项核心改动（commit 0b22ec0）

1. **波数监控 per-window 独立配置**：`GmWin` 含 region/speak/speak\_prefix/speak\_suffix/key\_waves，`GmCfg` 仅 windows+interval\_sec。前端配置键 `tfjl_game_monitor_cfg` 存 `winConfigs{标题:配置}`，旧全局格式自动迁移平移到每个已勾选窗口。
2. **阵容导入读取本地项目分类修复**：根因是调用了未定义函数 `_lineupProjectListFull()`，改用 `loadProjectListFromDB()`（IndexedDB 读取），并在下拉框补 categories 数组。
3. **升级回退链三级化**：v2.0.24 及更早无 `start_installer_silent` 时自动回退：写 .bat（带 `/S /R`）+ `open_url` 执行 → `start_umi_ocr` 兜底，确保老包也不弹 NSIS 安装向导。

* **测试**：15 个测试套件 596 断言全绿。修改文件：app-features.js、app-local2.js、src-tauri/src/lib.rs。

* **注意**：Cargo.toml 与 tauri.conf.json 版本曾漂移 5 个版本（2.0.20 vs 2.0.25），本次已同步。Rust 侧改动需重新打包 APP；前端改动部署后 Ctrl+F5 刷新即生效。

### 10:58 · 同版本覆盖问题核实与 2.0.26 决策

* 核实本地与 Gitee 上 v2.0.25 安装包哈希一致。

* **结论**：Tauri updater 仅在远端版本高于本地版本时触发更新，同版本覆盖需删 Gitee 旧资产重传并重新签名 updater.json，不如直接 bump 到 2.0.26。

* 2.0.26 可让 2.0.24 和 2.0.25 用户正常触发升级，且新包带 `start_installer_silent` 静默命令实现无窗安装。

### 10:48 · 三问题修复（波数独立配置/导入分类/升级回退链）

* **波数监控重构**：每窗口独立 region/speak/prefix/suffix/key\_waves，面板新增窗口选择下拉，77 项测试通过。

* **阵容导入**：根因是 `_lineupProjectListFull()` 未定义 → 改用 `loadProjectListFromDB()`。

* **升级回退链**：老包无 `start_installer_silent` 时写 .bat + open\_url 执行 → `start_umi_ocr` 兜底。

* 15 套 596 断言全绿。

### 09:55 · 发布 v2.0.25（commit 616ad4f）

* 升级回退链修复版，updater+pages 部署，installer 仅传 Gitee release。

### 09:48 · 阵容导入对话框对齐主选择器（commit 0c2c2ab）

* 分类下拉默认=当前分类（不做"全部分类"过滤），项目列表只列该分类项目+新项目置顶，当前项目排第一。项目导入弹窗文案统一。

### 09:43 · 升级弹窗问题修复（commit 921f0d9，v2.0.25）

* **根因**：前端 25 秒超时兜底太短，慢下载被误判失败；回退链借 Umi-OCR 启动命令跑安装包未带 `/S` 静默参数。

* **修复**：新增静默安装命令 `start_installer_silent`（传 `/S /R`，装完自动重启、零双开），超时从 25s 延长至 150s，升级前自动保存编辑中的项目数据，2.0.24 及更早版本回退旧方式自动兼容。

* **测试**：新增 17 项断言，16 套全绿，cargo check 无 warning。

### 09:39 · 升级回退链修复提交

* 三个入口（顶部提示/底部徽标/菜单）升级逻辑统一：先原生静默升级，失败才回退。

* Tauri 安装器内建静默机制：`/S /R` 参数装完自动重启、零双开风险。

### 09:20 · 安装界面差异解释

* **APP 内升级提示**走自动更新通道，`installMode=quiet` 带 `/S` 静默参数后台安装。

* **手动双击 setup.exe** 会弹 NSIS 安装向导，不会带静默参数——属正常现象，非 bug。

* tauri.conf.json 仅版本号变更，NSIS 段/updater 段/Rust 侧 download/install 逻辑均无改动。

### 09:17 · 发布 v2.0.24（commit e2de713）

* 签名校验 keynum 与信任根一致，直链自检 HTTP 200 且字节数一致。

* 远端 CI 在 version.json 写入 frontVersion/deployTag 字段。

### 09:05 · bump 2.0.24（commit e3f33f5）

* 波数监控是 Rust 侧新命令，必须装 2.0.24 新包才有。

* Cargo.toml 的 2.0.20 只是 crate 名义版本，不影响安装包号（但曾漂移 5 个版本需注意）。

### 08:52 · 波数监控升级（commit fe61293）

* **核心变化**：

  1. 监控循环下沉至 Rust 独立线程，关窗/最小化仍可播报
  2. 托盘图标显示金色数字角标指示当前波数
  3. 多窗口同时监控，播报带「N号窗」前缀
  4. 自定义播报文字（前缀/后缀，带预览和试听）
  5. 关键波数设置（如"10,20,25"）防连播噪音
  6. 优化截图区域：仅截波数小区域（几十 KB）直传 OCR，不再整窗截图+前端 canvas 转码
  7. 面板底部新增「到波自动执行操作 · 鼠标联动 · 分组播报…（其他功能尽情期待）」预告

* **重构**：umi\_ocr 拆出 HTTP 核心函数，供监控线程直接调用。

* **测试**：15 套 573 断言（监控测试重写为 77 项），cargo check + pre-commit 36 命令通过。

### 08:19 · 统一导入流：分类优先（commit 27409ca）

* 阵容导入弹窗对齐主选择器——分类下拉默认当前分类，项目列表只列该分类，新项目置顶，当前项目第一。

### 07:48 · 紧凑单图分享布局（commit 9ab8be0）

* 900px 深色渐变分享图，项目名+分类副标题+我方/队友 7 格含工程位+双方手牌 10 张+版本水印，剪贴板多导入支持。

### 06:35 · 修复大版本提示消失（commit 1c294b3）

* **根因**：底部常驻提示被整体删除。

* 大版本检查仅在 APP 启动时执行一次且无轮询机制。

* **修复**：恢复底部常驻提示 + 实现静默升级 + 添加运行中复查机制。

* 14 套测试全绿，新前端版本 s1.0.574。

### 06:03 · v2.0.23 发布完成

* CI 自动提交与发布脚本在 version.json 上产生冲突（已记录进铁律，本次 publish\_update.ps1 自愈逻辑的前身）。

* 老用户升级路径：打开软件后通过右下角提示进行静默安装。

### 05:57 · 发布 v2.0.23（commit d60d3b1）

* updater+pages 部署，installer 仅传 Gitee release。

* 软件大小动态读 version.json.size 字段。

### 05:50 · 项目分享 8 位短码（commit 80ca00f）

* 8 位短码 + 有效期 + 密码选项，阵容 20 张（7 格+工程位+双方手牌），导入可选目标项目避免覆盖当前项目，Gist 管理器识别分享类型显示过期状态。

### 05:51 · bump 2.0.23（commit 0ed2b8f）

### 04:54 · webroot 精简 + 阵容分享闭环（commit d94f090，功能 5+6）

* **webroot**：精简为单个占位跳转页（\~1KB），消除副本维护隐患。Tauri frontendDist 必须指向有效目录故保留 index.html 占位，APP 运行时直连远端（tauri.conf.json windows.url）。

* **verify\_build.js**：移除 webroot 下 JS 死引用，只保留占位页 div/内联检查。

* **阵容分享闭环**：

  * 分享时把阵容 payload 写进公开 Gist 的 lineup.json 得 `#lg=<gistId>` 短链（\~80 字符）

  * qrcode-generator 懒加载（vendor/，`_lineupEnsureQrLib` 有失败重置可重试）后带二维码重画分享图

  * 接收三条路：扫码直达网页版自动弹导入 / 打开 `#lg=`·`#lineup=` 链接自动弹导入 / 导入弹窗直接粘贴整条链接（OK 按钮双路正则解析）

  * 短链或二维码失败自动降级为纯阵容码+长链，复制链接用固定 GitHub Pages 域而非 location.origin

* **测试**：97 项全绿，其余 13 套件无回归。

### 04:32 · 阵容分享闭环上线（功能 5+6）

* 见 04:54 条。

### 04:18 · webroot 精简为占位跳转页（commit f503ed9）

* 见 04:54 条。

### 04:08 · 一键识别游戏画面阵容（commit 89f22e0，功能 10）

* 自动找窗口截图直接进入识别流程，复用现有 OCR 能力。

### 04:02 · 阵容分享入口统一到 app-features.js（commit 14f7808）

* 1080px 完整版实现统一入口。

### 03:45 · 游戏窗口检测+波数识别+语音播报三合一（commit 40a1910，功能 8+9）

* Windows API 枚举窗口按标题匹配找到游戏窗口，获取句柄和位置。

* 波数识别：定时截图（每 2-3 秒）+ Umi-OCR 识别固定区域波数文字。

* 波数播报：Windows 自带 TTS，波数变化时触发。

* 波数位置配置：首次使用时鼠标框选波数显示区域并保存，同一分辨率下无需重复配置。

* **后续已升级**：见 08:52 条（监控下沉 Rust、多窗口、per-window 独立配置）。

### 03:21 · 阵容分享文案按钮（commit 0346d3b）

* 「复制文案」生成纯文字阵容（分类+项目名+我方/队友槽位含工程位标记+双方手牌），用于 QQ/微信群讨论，clipboard API + execCommand 兜底。

### 03:11 · 设置面板布局调整（commit 34d44aa）

* 新顺序：①车主副本开车统计 ②对战统计 ③扫描到的脚本文件 ④备份与还原 ⑤老马脚本目录配置（默认折叠，底部）。

* 老马脚本目录配置内含临时脚本目录、软件数据目录。

* 自动加载开关、强制刷新按钮、恢复默认目录按钮位置不变。

### 02:52 · 融合卡推送失败根因 + 切皮增量 + 单卡切皮（commit cd828cb）

* **推送失败根因**：本地提交未推送导致 git 分叉，CI 自动提交缓存版本号更新后产生冲突，非快进推送被拒绝。

* **自愈推送**：`push_with_self_heal`——push 被拒时自动 `pull --rebase --autostash` 重推一次，autostash 保护工作区未提交改动。

* **推送范围扩大**：`git_push_fusions` 提交范围扩到 fusions.json + registry.json + skins/融合XX/ 一起（原只推 fusions.json，切皮产物需手动去皮肤 tab 二次推送，易漏）。

* **切皮三合一**：抽取 `cgmCutFusionSkins(subHero, force)` 单卡切皮函数，已切过的（registry 有融合名 XX 记录）默认跳过，`force=true` 强制重切。

* **融合卡列表**：每行新增「切皮」按钮（强制重切该卡副卡全部皮肤，带 loading/结果反馈）。

* **一键全切增量模式**：已切自动跳过、只切新增，进度条区分新切/跳过/失败。

* **添加后自动切皮**：仅新添加的单张融合卡自动切皮。

* **成功文案**：从「请手动终端跑 git 命令」改为「引导点一键推送按钮」。

### 02:45 · 统计自动加载 + 阵容分享图 + 窗口置顶 + 目录调整（commit 3821db2）

* **统计自动加载修复**：统计计算改 requestAnimationFrame 下一帧执行（面板 DOM 挂载完成再跑，避免元素未就绪静默失败）+ try-catch 防单统计异常中断另一个，统计开关保留（低配电脑可关闭防卡，关闭时显示占位提示）。

* **阵容一键分享图**：出战选择区新增「📸 分享」按钮，canvas 绘制 900px 深色渐变图（项目名+分类+我方/队友 7 格含工程位+双方手牌 10 张+卡牌皮肤图+版本水印），弹窗预览+💾 保存到数据目录\share\ 文件夹（自动弹资源管理器定位）+📋 复制图片到剪贴板直接粘贴 QQ/微信，网页版降级为浏览器下载。

* **Rust 新增 show\_in\_folder 命令**（explorer /select 选中定位）+ 注册 toml 权限 + capability。

* **窗口置顶功能**：标题栏📌按钮，core:window:allow-set-always-on-top 权限。

* **剪贴板导入脚本功能**。

* **APP 本地设置布局**：统计项移到备份与还原上方，临时脚本目录/软件数据目录收进老马脚本目录折叠区。

* **更新目录调整**：D:\withfriends\塔防精灵助手更新 移到 D:\withfriends\塔防精灵助手数据\update 子目录归档（Rust 端 + 前端同步）。

* **skins/融合孤星**：融合孤星的两张皮肤入库（上次融合添加遗漏）。

### 02:22 · 卡组管理一键推送 skins/fusions.json（commit e6039a1）

### 00:49 · 铁律补充 dragDropEnabled 双保险文档（commit a0e04dc）

* 在 [铁律.md](file:///d:/tfjl-web/铁律.md) 添加 A5（发版前检查 dragDropEnabled=false）、E5（发版后 APP 实测手牌拖拽）、故障速查表新增「APP 里手牌拖不动/拖了放不下」一行。

* 红线：前端分流代码（app-core.js updateHandDisplay 内）永远不许删，存量老版本（≤2.0.22）全靠它拖。

### 00:39 · 导入弹窗两级选择 + APP 手牌拖拽根因修复 + 下拉框滚轮切换（commit 82b8a14）

* **导入/保存弹窗两级选择**：分类下拉（全部分类合并 categories 与项目动态分类，默认=当前项目所在分类）+ 项目下拉（只列该分类项目，当前项目默认选中），saveScriptCategoryChange 联动；旧版 optgroup 分组挤一个下拉的选项体验差。

* **APP 手牌拖拽根因**：Tauri 默认 `dragDropEnabled=true` 拦截 WebView2 的 HTML5 DnD（drop 落不了地），且原生 drag 一启动就 pointercancel 掉 Pointer 事件流 → 原生/兜底两层全断。

  * 修复①：前端检测 Tauri 环境时手牌 `draggable=false`，全走 Pointer 兜底层（线上 JS，对所有 APP 版本立即生效）

  * 修复②：`tauri.conf.json` 加 `dragDropEnabled:false`（下个 APP 版本双保险恢复原生 DnD 能力，只对新版本生效）

* **下拉框滚轮切换**：`attachSelectWheel` 通用函数——鼠标停在 select 上滚轮即换选项，跳过空值与 \_\_ 开头特殊项（避免误触弹窗），边界不循环，change 事件派发后才有联动。

* **测试**：扩展至 63 项（E 两级选择 12 项 / F APP 拖拽 6 项 / G 滚轮 9 项）+ test-slot-swap-skin.mjs window mock+APP 环境分支断言 22 项，11 套 285 断言全绿。

### 00:18 · 脚本导入项目通用化 + 手牌拖拽第三重自愈（commit 752be0c）

* **统一记事本模板加「📥 导入项目」按钮**（`isScan||isLocal` 双条件）：扫描文件（APP 本地设置）/ 项目内文件均可一键把当前脚本内容（含标色 marks）另存副本到指定项目——stash 内容/名称/marks 后复用 showSaveScriptDialog 项目选择弹窗，源文件不动；需求墙远程脚本已有同步款「保存副本」不重复加。

* **三入口汇聚**：扫描（openScannedInNotebook）/项目（openTxtFileWindow）/远程三条入口全部汇聚到 `openScriptNotebook`，一处实现处处可用。

* **弹窗三修**：a) z-index 固定 10000 被扫描文件查看浮窗（100000+）压住 → 改动态 200000+ topWinZIndex 永远盖顶；b) 空内容防误：stash 来源用 hasOwnProperty 判定（含空串），不再误回退上次生成器残留的 \_activity/\_dungeonScriptOutput 把陈旧内容导入项目；c) 默认文件名：源名.txt 结尾直接沿用原名，否则带日期生成。

* **doSaveScriptToProject toast 文案**：去掉写死的「需求墙」改通用「源文件不变」。

* **手牌拖拽第三重自愈**：pointerdown 首行重置 `__nativeDrag=false`——合成/异常取消的 dragstart 无 dragend 兜底时，标志位残留 true 会让 pointerup 首行稳妥吞掉 Pointer 兜底层；真实原生拖拽进行中浏览器吞掉 pointerdown 不存在这问题。

* **测试**：新增 tools/test-script-import-drag.mjs（33 项断言：按按钮条件/stash 三件套/弹窗判定/层级动作/文件名沿用/入库 marks/覆盖提示/拖拽自愈+稳妥保留+b68f7b5 draggable 修改无回退），11 套 255 断言全绿。

### 00:00 · 当日工作起点

* 接续昨日（2026-08-30）的手牌拖拽与脚本导入功能，进入新一轮迭代。

***

## 历史日志

> 2026-08-31 之前的日志未单独归档，可在 memory 的 topics.md / session\_memory\_\*.jsonl 中回溯：
> `c:\Users\gyq\.trae-cn\memory\projects\-d-tfjl-web--p2-a6d065a4078d0cabcabd\<YYYYMMDD>\`

