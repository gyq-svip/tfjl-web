# 铁律（项目约定，违反即视为 bug）

<p style="color:red;font-weight:bold;font-size:1.4em;">📦📦📦 打包发布标准流程（小白教程 · 每次发版严格照做 · 2026-08-29 立）📦📦📦</p>

> **写在最前面**：自动升级出过 3 次事故（2026-08-28 深夜折腾数小时），全部源于"打包环节没按流程走"。
> **从今往后，发版只准照下面 A→B→C→D 四步做，一步都不能跳、不能改顺序。**

## A. 发版前检查（3 分钟，必做）

在仓库根目录 `d:\tfjl-web` 打开 PowerShell，逐条跑：

**A1. 确认工作区干净**

```powershell
git status --short
```

* 有输出 = 还有未提交改动 → 先提交（见 A2）或确认无误。

**A2. 确认当前版本号**

```powershell
node -e "console.log(require('./src-tauri/tauri.conf.json').version)"
```

记下这个号，下一步要 +1。

**A3. 检查 updater 配置（出过事故，必查）**

```powershell
node -e "const j=require('./src-tauri/tauri.conf.json');const u=j.plugins.updater;console.log('version='+j.version);console.log('endpoints='+JSON.stringify(u.endpoints));console.log('installMode='+JSON.stringify(u.windows))"
```

**必须是这样，否则自动升级必然失效：**

```
endpoints   = ["https://gyq-svip.github.io/tfjl-web/updater.json"]
installMode = {"installMode":"quiet"}
```

* ❌ `endpoints` 里**绝对不许**出现 `{{current_version}}`！
  （历史事故：用了 `.../releases/download/v{{current_version}}/updater.json`，
  每个版本都读到"自己那份"，Tauri 永远判定"无更新"，原生升级形同虚设。）

* ❌ `installMode` 不许是 `passive`（会弹 NSIS 安装窗口）。必须是 `quiet`（完全静默）。

**A4. 检查打包 exe 与签名 key 是否匹配**（脚本会自动查，这里先确认文件在）

```powershell
Get-ChildItem src-tauri\target\release\bundle\nsis\*.exe | Select-Object Name,LastWriteTime
```

**A5. 🔴 检查 dragDropEnabled = false（2026-08-31 新增，防 APP 手牌拖拽回归）**

```powershell
node -e "const j=require('./src-tauri/tauri.conf.json');console.log('dragDropEnabled='+j.app.windows[0].dragDropEnabled)"
```

**输出必须是** **`false`，不许删这行配置！**

* **背景**：Tauri 默认 `dragDropEnabled:true` 会拦截 WebView2 的 HTML5 拖放（drag 启动了但 drop 落不了地），
  且原生 drag 一启动就 pointercancel 掉 Pointer 事件流 → 原生/兜底两层全断。
  这就是「网页版手牌能拖、APP 版拖不动」的元凶（2026-08-31 修复）。

* **双保险结构（两层都不能拆）**：

  * ① **前端分流**（`app-core.js` updateHandDisplay 内，搜注释 `Tauri 默认 dragDropEnabled=true 会拦截`）：
    检测到 Tauri 环境时手牌 `draggable=false`，全走 Pointer 兜底拖拽层。**线上 JS，对所有 APP 版本立即生效（含老版本）。**

  * ② **`tauri.conf.json`** **的** **`dragDropEnabled:false`**：恢复 WebView2 原生 DnD 能力。**只对打包后的新版本生效**。

* **🔴 红线：① 的前端分流代码永远不许删！**
  老版本 APP（≤2.0.22）没有 ② 配置，原生 DnD 仍被拦截，全靠 ① 才能拖。
  没有强制升级机制，存量老版本用户会长期存在 → ① 必须永久保留。
  ② 的意义：将来若有人手贱把 ① 删了（想恢复原生拖拽体验），有 ② 兜底不至于复现「拖得动放不下」死局。

* **副作用核查（2026-08-31 已核）**：`lib.rs` 无 `onDragDrop` / 原生 drag-drop 事件依赖，此配置零副作用。
  将来若做「拖文件进窗口导入」功能，用 HTML5 `drop` 事件实现（原生 drag-drop 事件已被此配置禁用，HTML5 反而恢复可用）。

## B. bump 版本号（单独 commit，铁律第 6 条）

**版本号必须单独一个 commit，禁止混进功能改动里！**

```powershell
# 把 2.0.19 换成「当前版本 +1」
node -e "const fs=require('fs');const p='src-tauri/tauri.conf.json';let t=fs.readFileSync(p,'utf8');t=t.replace(/\"version\":\s*\"[^\"]+\"/,'\"version\": \"2.0.19\"');fs.writeFileSync(p,t)"
git add -A
git commit -m "chore: bump version to 2.0.19"
```

> ⚠️ PowerShell 里**不要用中文 commit message**（编码会炸成乱码报 `字符串缺少终止符`）。
> commit message 一律用英文。

## C. 打包 + 签名（职责：AI 打包 + 发布，用户只签名）

> **🔴 职责铁律（2026-08-29 修订，最高优先级）**
>
> * **AI 负责**：C1 打包（`npx tauri build`）、D 发布（`.\publish_update.ps1`，全自动）。
>
> * **用户只负责**：C3 签名（`.\sign.ps1`），本机 3 秒完成。
>
> * **用户不跑任何 Copy-Item**（sign.ps1 内部已自动复制成英文包名，见第 88-90 行）。
>
> * **用户不跑 publish\_update.ps1**（归 AI）。

**C1. 打包（AI 执行）**

```powershell
npx tauri build
```

等待完成。产物在：

```
src-tauri\target\release\bundle\nsis\塔防精灵助手_<版本>_x64-setup.exe
```

AI 打包完成后，把下面 C3 命令打给用户，用户回"签好了"后 AI 才跑 D。

**C2. 复制 exe 到根目录 —— ❌ 已废弃，用户不用做**

> `sign.ps1` 第 88-90 行已自动把中文包复制为根目录英文包名 `tfjl-assistant_<ver>_x64-setup.exe`，
> `publish_update.ps1` 直接从根目录取。用户**绝不手动 Copy-Item**。

**C3. 签名 —— 🔴 由用户手动执行，AI 不准代跑（2026-08-29 立规）**

> **为什么**：用户本机跑 `sign.ps1` 只要 **3 秒**；AI 跑要 **数分钟**（密钥恢复/环境初始化差异）。
> **规矩**：AI 打包完后，只负责把下面命令「打出来」给用户复制，用户自己回车执行；
> 用户回一句"签好了"之后，AI 才继续做 D 步（发布）。

用户请复制执行（在 PowerShell 里）：

```powershell
cd d:\tfjl-web
.\sign.ps1
```

看到 `Sign success and verified!` 才算成功。
脚本会自动：恢复 `tauri.key` → 跑 `verify_build.js` 校验 → **复制中文包为根目录英文包名** → 复制成 ASCII 临时名签名 → 校验 pubkey keynum。

> ❌ 签名失败/keynum 不一致 → **停止**，AI 重跑 `npx tauri build` 后用户重试，绝不跳过。
> 🤖 **AI 注意**：不要自己执行 `.\sign.ps1`，只输出命令文本等用户反馈。

## D. 发布（写 updater.json + 传 Gitee + 触发 Pages 部署）—— 🔴 由 AI 执行

```powershell
.\publish_update.ps1
```

AI 在用户"签好了"之后运行。它会依次做：

1. 生成根目录 `updater.json` / `version.json`（版本号取自 `tauri.conf.json`）
2. 上传 exe + updater.json 到 **Gitee 发行版 v<版本>**
3. `git commit` + `git push origin main`（触发 GitHub Pages 部署 + 更新线上 updater.json）

看到 `Published: v<版本>` 即成功。脚本最后会自动验证 Gitee 下载直链（HTTP 200 + 大小一致）。

> ⚠️ **Gitee 代码树不会自动同步**（脚本只 push GitHub）。
> 所以 `gitee.com/.../raw/main/updater.json` 会长期停留在旧版本，**不能**用作 updater endpoints。
> 若将来要让索引也走 Gitee，必须先改脚本同时 push Gitee，再换 endpoints。

## E. 发版后验证（5 分钟，必做）

**E1. 确认线上索引已更新**

```powershell
curl.exe -s "https://gyq-svip.github.io/tfjl-web/updater.json"
```

`version` 字段必须 = 刚发的版本号。

**E2. 确认 GitHub Pages 部署完成**

```powershell
curl.exe -s "https://gyq-svip.github.io/tfjl-web/index.html" | Select-String "app-features.js"
```

**E3. 用老版本实测升级**
装一个**比你刚发的版本旧**的包（例如发的是 2.0.19，就装 2.0.16），然后：

* 打开 → 右下角出现 `🔔 新版本 v2.0.19`

* 点它 → 应出现 `⏳ 正在下载更新包…` → `🚀 正在启动安装程序…`

* 安装程序启动 → 完成 → App 自动重开成新版本

**E4. 调试日志核对**
在 App 调试窗跑 `checkAppLocal()`，确认：

```
openAppLocalSettings: "function"
btnDisplay: "flex"
```

**E5. 🔴 APP 里实测手牌拖拽（2026-08-31 新增，配合 A5）**
升级到新版后，在 App 里把**我的手牌**一张卡**按住拖到卡槽**：

* 必须能正常上卡（Pointer 兜底层路径，老/新版本都靠它）。

* 再拖一次**卡槽卡互换**（卡槽→卡槽），确认互换正常。

* 若拖不动 → 先强刷（双击右下角版本号），仍不行则检查前端分流是否被误删（见 A5 红线）。

## 常见故障速查

| 现象                                    | 原因                                | 解决                                                               |
| ------------------------------------- | --------------------------------- | ---------------------------------------------------------------- |
| 点升级没反应 / 永远"无更新"                      | endpoints 含 `{{current_version}}` | 按 A3 改成固定 GitHub Pages 地址                                        |
| 升级弹 NSIS 安装窗口                         | `installMode` 是 `passive`         | 改成 `quiet`（需重新打包）                                                |
| `schannel: SSL/TLS connection failed` | git 走系统代理握手失败                     | 用 `git -c http.sslVerify=false push origin main`                 |
| `Updates were rejected (fetch first)` | CI 自动 commit 了 sw\.js             | 先 `git -c http.sslVerify=false pull --rebase origin main` 再 push |
| PowerShell 报"字符串缺少终止符"                | 命令里含中文                            | commit message / 参数改用英文，或用 `git add -A` 避开中文文件名                  |
| 某 JS 文件所有导出都 undefined                | 该文件有 TDZ 错误（const 用在前、声明在后）       | 见第 13/14 条                                                       |
| APP 里手牌拖不动 / 拖了放不下                    | `dragDropEnabled` 被删（或前端分流被误删）    | 见 A5：配置改回 `false` + 确认 `app-core.js` 前端分流在                       |
| 前端改了用户没生效                             | Service Worker 缓存                 | App 里双击右下角版本号，或调试窗跑 `clearSWCache()`                             |

***

<p style="color:red;font-weight:bold;font-size:1.2em;">🔴🔴🔴 铁律第一条（最高优先级，违反即视为严重 bug）🔴🔴🔴</p>

<p style="color:red;font-weight:bold;">❗❗❗ 绝对禁止修改、删除、重构、或任何方式动右下角的版本号标记（#versionTag）及其 tooltip、绑定、文案、弹窗相关逻辑！❗❗❗</p>

<p style="color:red;font-weight:bold;">❗ 双击 #versionTag 即可直接更新，原本就能用，不需要任何弹窗、不需要"点击检查更新"、不需要改 tooltip 文案、不需要加 onclick！❗</p>

<p style="color:red;font-weight:bold;">❗ 该标记的渲染、文案、s日期-HHMM · s1.0.xxx 格式全部由 CI 自动注入（见下方说明），任何提交都不许手动改它！❗</p>

<p style="color:red;font-weight:bold;">❗ 之前 AI 擅自把 #versionTag 的 ondblclick 改成 onclick、加"点击检查更新"弹窗、改 tooltip 文案，全部已被 revert 还原。今后谁再动这里，立即 revert！❗</p>

> 本文件为 tfjl-web 强约束。任何涉及 GitHub Gist / Token / 统计读写 / 自动发布的改动，必须先读本文件。
> 部署方式：`deploy.yml` 监听 `push: [main]`（及 `workflow_dispatch`），push 到 main 即自动部署到 GitHub Pages；token 由该 workflow 的 "Inject Token" 步骤注入。
> 每次部署 CI **自动**做两件事（无需手改源码）：① bump 所有脚本 `?v=` 缓存版本号（cache-bust，防 Service Worker 跑旧 JS）；② 把页面右下角 `#versionTag` 注入本次部署时间标记，格式 `s日期-HHMM · s1.0.xxx`（如 `s20260824-2033 · s1.0.230`）。**前半段** **`s日期-HHMM`** **= 发布/部署时钟时间（即你要的"发布时间"），每次部署都变，凭此判断最新；后半段** **`s1.0.xxx`** **=** **`sw.js`** **的** **`CACHE_VERSION`** **语义版本号（如 230），是缓存大版本、不是部署次数，禁止改成部署序号**。**判断"是否最新版"以线上页面右下角实时标记为准，不是看本地** **`index.html`** **的静态值**。

## 一、Gist 所有权与读写规则（最高优先级）

1. **所有 Gist 都属于** **`gyq-svip`** **这个账号自己创建的**（用户明确确认：所有 Gist 都是 gyq-svip 账号手动/自动创建的，不存在"别人的 Gist"）。

   * 所有 Gist ID 在代码中**硬编码**，禁止代码自动发现/自动创建陌生 Gist。

   * 计数器 Gist：`COUNTER_GIST_ID = 'e1bd9a5139e1c4e011bfea707e917d61'`（归 gyq-svip）

   * 索引 / 消息 / 广播等 Gist：同理，ID 必须硬编码或来自你已知清单，不得运行时凭空生成。
2. **禁止"自动创建新 Gist 并缓存到 localStorage 然后长期使用"**。代码里凡是
   `localStorage.setItem('counter_gist_id', responseData.id)` 或从 `index['counter']` 覆盖 `COUNTER_GIST_ID`
   的逻辑，一律删除——这是"出现非我创建的 Gist"的唯一来源。
3. **读写策略（默认你有权限，不做多余预检，仅对真实响应兜底）**：

   * Token 由部署自动注入（见第 4 条），**代码不要做"先验我有没有权限再放行"的额外关卡**——拿到 token 直接去读写，正常情况一路畅通，不耽误事。

   * 仅当 GitHub 真实返回 **401/403/404** 时才处理：

     * 明确 `console.warn('[Gist] <用途> 读写失败 status=<code>，请检查 Token/权限')`

     * **立即 return / 停止重试**，**绝不允许**转而创建匿名新 Gist 或用别的 Gist 顶替（避免写出归匿名的 Gist 导致永久不可读写）。

   * 说明：401 判断是安全网不是障碍——它能在 token 注入失效时立刻暴露问题（如 2026-08-24 限额变 60 事故），删掉反而会让代码静默用匿名身份写、产生你永远读不回的 Gist。
4. Token 来源（**写死，勿改**）：App 端与 Web 端**完全一致**——均为源码顶部的 `HARDCODED_TOKEN = 'YOUR_GITHUB_TOKEN_HERE'` 占位符，**由 `deploy.yml` 的 "Inject Token" 步骤在构建/部署时统一替换为 `secrets.GIST_TOKEN`，自动注入。无任何手动配置、无本地文件落盘，用户从未、也无需手动填写任何 token**。`HARDCODED_TOKEN` 占位符以 `ghp_` 开头且 length>20 才生效，原文 `YOUR_GITHUB_TOKEN_HERE` 不生效。
  * ⚠️ **纠正错误认知（硬约束）**：Web 端与 App 端写入 GitHub Gist 的权限**完全相同**（都持有注入的同一 token）。凡"Web 没有写入权限""Web 端依赖本地配置 token""Web 分享需降级为仅限 App"等说法**均属无稽之谈**。遇到"Web 写入失败"必须当真实 bug 排查（限额 / 代码路径 / CORS / 与其他能写的功能对比），**绝不可归因为"Web 无权限"而建议降级或仅限 App**。
  * 旧版曾列"② `localStorage['TFJL_Gist_Token']`（本地开发/用户手动填写）；③ iframe 父窗口注入"作为来源——实际用户从未手动配置过 token，这两条仅为极早期兜底分支，**不是正式 token 来源**，不得据此推断"Web 需本地配置 token"或"App 有本地 token 而 Web 无"。

   * **严禁删除** **`HARDCODED_TOKEN`** **占位符及其注入逻辑**——2026-08-24 曾因全仓去敏误删该占位符，导致部署后 `getGistToken()` 永远返回空（API 监控 token 全 X、rate\_limit 限额变 60、需求墙/分享误报离线）。`deploy.yml` 第 84-87 行仍在按 `YOUR_GITHUB_TOKEN_HERE` 做 sed 替换，源文件占位符缺失会导致注入静默失败。

   * 用户明确确认：**"我的 token 是自动注入的，占位符不能删除的"**——这是硬约束。今后任何去敏 / 重构 / "死代码清理"都**绝不可删** `HARDCODED_TOKEN = 'YOUR_GITHUB_TOKEN_HERE'` 及 `getGistToken` 里的占位符读取分支。判断某段像死代码前，必须先确认是否有外部注入/构建脚本（deploy.yml 等）依赖它。

   * Token 缺失/失效时相关写操作**静默跳过 + 明确 WARN**，不得抛未捕获异常、不得崩溃刷屏。

## 二、版本号与发布

1. `version.json` 的 `version` 必须与 `src-tauri/tauri.conf.json` 的 `version` 一致，由 `publish_update.ps1` 统一写入，禁止手改两处不同步。
2. 版本 bump 必须**单独 commit + push**（铁律发布流程），禁止把版本号混进功能提交。

## 三、Tauri 命令授权（权限铁律）

1. 任何新增/重命名 Tauri 命令，必须**三处同步**：`src-tauri/src/lib.rs`（invoke\_handler）、
   `src-tauri/capabilities/allow-custom-commands.toml`（自定义权限 identifier，如 `allow-read-text-file`）、
   `src-tauri/capabilities/default.json`（capability 引用 identifier）。三处缺一则打包失败。

   * 注意：capability 引用的是 toml 里 `[[permission]]` 的 `identifier` 字段，**不是** Rust 命令名。
2. 每次打包前必须跑 `.github/verify_build.js` 全量校验（含"23 命令三处一致"），不通过不得打包。

## 四、网页与 APP 一致性

1. ~~任何影响网页端的功能改动，必须同步~~ ~~`webroot/`~~ ~~同名文件~~ **【2026-08-29 废止】**

   * `webroot/` 已于 2.0.21 精简为**纯占位跳转页**（仅 `index.html` 一句跳转 GitHub Pages），
     不再内嵌任何前端 JS/CSS。`tauri.conf.json` 的 `app.windows[0].url` 指向线上，
     App 运行时直连 `https://gyq-svip.github.io/tfjl-web/`，**不读取 webroot 内的代码**。

   * 因此：**前端改动只需推 GitHub Pages（网页端/App 通用），无需再同步 webroot 双份。**

   * 若需变更占位页内容，只改 `webroot/index.html` 一个文件即可。
2. 下载桌面版弹窗的"软件大小"动态读取 `version.json.size` 字段，由 `publish_update.ps1` 自动写，
   不要手写死（无字段时回退静态文案）。

## 五、错误处理

1. 任何 `try/catch` 外的变量必须 `let/const` 声明；禁止隐式全局（曾因 `content` 未声明导致
   `ReferenceError: content is not defined` 掩盖真实 401）。
2. 调试日志面板（天使日志）必须全量收集 `console.*` + `window.onerror` + `unhandledrejection`，
   与 F12 Console 一致。浮窗开关只存在于调试日志面板内，不另占管理员菜单。

## 六、🔴 const/let 声明顺序（TDZ）——新增功能插常量时必查（2026-08-28 血案）

1. **`const`** **/** **`let`** **必须先声明、后使用。往文件里新插常量前，必须确认下方更早位置是否已在引用它。**

   * 违反会抛 `ReferenceError: Cannot access 'XXX' before initialization`（TDZ 暂时性死区），
     **且该错误会让整个 JS 文件静默中断**，表现为"该文件所有 `window.*` 导出全部失效"。

   * **2026-08-28 事故**：`app-local2.js` 第 529 行 `settingsConfig = { ... autoBackupKeep: AUTO_BACKUP_DEFAULT_KEEP ... }`
     使用了常量，但 `const AUTO_BACKUP_DEFAULT_KEEP = 20` 声明在 **543 行（后面 14 行）**。
     → 整文件中断 → 4307 行 `window.openAppLocalSettings = ...`、`window.scanSkins` 等全部没挂上
     → App 里"APP设置"按钮 `display:none`、点击报 `openAppLocalSettings is not a function`。
     根源是当天新增"自动增量备份"功能时，把 `AUTO_BACKUP_*` 整批常量插到了 `settingsConfig` **之后**。

   * 修复：常量声明整批前移到 `settingsConfig` 之前（根 + `webroot/` 的 `app-local.js`、`app-local2.js` 四份同改）。

2. **这类错误的危害在于"症状完全不像声明问题"**：只表现为"功能失效/导出没了/按钮不显示"，
   控制台仅一闪而过一行 ReferenceError，极易被误判成 **Service Worker 缓存 / 部署不对 / 文件残缺**。
   本次就因此白排查数小时（反复 clearSWCache、重新打包 2.0.18、卸载重装，全部无效——
   因为 `curl` 线上文件一直是好的，导出齐全，根本不是缓存问题）。
   **凡遇"某 JS 文件的导出全部 undefined"，第一嫌疑就是该文件头部有 TDZ / 运行时错误，先查代码顺序，别急着怪缓存。**

3. **改了** **`app-*.js`** **等被** **`index.html`** **用** **`<script defer src="xxx.js?v=s1.0.XXX">`** **引用的文件，必须同步 bump** **`index.html`** **里的** **`?v=`** **版本戳！**

   * `index.html` 第 936 行 `<script defer src="app-local2.js?v=s1.0.419">`、第 1557 行 `<script defer src="app-core.js?v=s1.0.315">` 等，
     这些 `?v=` 戳是**写死的旧值**，CI 部署不会自动改它们。

   * 若只改了 `app-local2.js` 内容却不动 `?v=` 戳 → 浏览器/ SW（StaleWhileRevalidate）认为 URL 没变 → 直接返回**缓存的旧 app-local2.js**
     → 用户跑"新版 sw\.js + 旧版 app-local2.js"混合体 → **APP设置按钮不显示 / 功能缺失**（2026-08-29 事故：推了 483/484 修复，用户更新后 APP设置消失，根因就是 `?v=` 戳还是 419）。

   * **修复动作（每次改这些脚本必做）**：把 `index.html` 里对应脚本的 `?v=s1.0.旧` 改成与本次 sw\.js `CACHE_VERSION`（CI +1 后）一致的值；
     同时 bump `sw.js` 的 `CACHE_VERSION` + `DEPLOY_TAG`，让 SW 清缓存拉新 `index.html`（新 index.html 指向新 `?v=` 戳的脚本）。

   * 切记：CI 只会 `cp app-*.js _site/`，**不会改** **`index.html`** **的** **`?v=`** **戳**——这是人工必做项。

## 七、Tauri App 前端报错排查手法（2026-08-28 确立）

1. **Tauri App 内 F12 打不开 DevTools；浮动调试窗的** **`floatConsoleExec`** **对长命令会被 eval 截断**
   （含 `.then()` 链的长句常报 `SyntaxError: Unexpected token ')'`，命令根本没执行、日志里连记录都没有）。
   → 排查 App 前端执行错误时，**改用** **`playwright-cli`** **开干净浏览器直接打线上 URL**：

   ```bash
   npm i -g @playwright/cli@latest
   playwright-cli open "https://gyq-svip.github.io/tfjl-web/?t=<随机>"
   playwright-cli console          # 读完整堆栈（本次就是靠它抓到 app-local2.js:529:122）
   playwright-cli eval "JSON.stringify({oa: typeof window.openAppLocalSettings})"
   playwright-cli dialog-accept    # 网页版点设置会弹「未找到Tauri invoke」提示，属正常
   playwright-cli close
   ```

   * **网页版也能验证 App 专属脚本的导出**：`app-local2.js` 的导出在 `if(true)` 块内无条件执行，
     浏览器里同样会挂上 `window.openAppLocalSettings`，不必非装 App 才能测。

   * 注意：`playwright-cli` 默认**不能**访问 `file://`（报 protocol blocked）；
     起本地 `http-server` 又常起不来时，直接部署后用浏览器打线上域名最省事。

2. `curl` 校验线上文件要用 **`curl.exe`**（PowerShell 里裸 `curl` 是 `Invoke-WebRequest` 别名，参数不兼容会报错）。
   核对导出是否齐全的标准做法：

   ```powershell
   curl.exe -s "https://gyq-svip.github.io/tfjl-web/app-local2.js" -o _tmp.js
   Select-String -Path _tmp.js -Pattern "window.openAppLocalSettings|window.runDiagnostics|var isTauriApp"
   ```

   推 GitHub 遇到 `schannel: SSL/TLS connection failed` 时，用 `git -c http.sslVerify=false push origin main`
   （保留本机 7897 代理，别用 `publish_update.ps1` 里的 `-c http.proxy=` 清空写法，那会直接超时）。

3. **每次改完** **`app-*.js`** **/** **`sw.js`** **等任意 JS 文件，必须跑** **`node --check <文件>`** **确认无语法错误！**

   * 2026-08-29 惨案：`app-local2.js` 出现**两个** **`async function _autoBackupNow(dir)`** **重复声明** →
     `SyntaxError: Identifier '_autoBackupNow' has already been declared` → **整个 JS 文件解析失败、完全不执行**。
     后果：`initAppLocal` 不注册 → APP设置按钮不显示；所有 `window.*` 导出（openAppLocalSettings / resolveHeroSkinUrl /
     scanSkins 等）全部没挂 → 所有皮肤（卡池/收藏/手牌/上阵）全光板。症状与 TDZ/缓存一模一样，但根因是**硬语法错误**。

   * 这种错误 `node --check` 一秒就能抓到（输出 `SyntaxError: Identifier 'XXX' has already been declared` 及行号），
     **但肉眼看不出来、缓存论/部署论都查不到**。凡遇"某 JS 文件所有导出全失效"，第一动作就是 `node --check` 该文件。

   * 根因来源：`replace_in_file` 改函数体时若 `old_str` 不唯一/被误匹配，可能把整块函数又插了一次 → 重复声明。
     **改动含函数新增/整块替换时，改完务必** **`node --check`**，并可用脚本扫描重复 `function`/`const` 声明（见下方）：

     ```powershell
     node -e "const fs=require('fs');const s=fs.readFileSync('app-local2.js','utf8');const m={};let r;const re=/^\s*(?:async\s+)?function\s+([A-Za-z0-9_]+)\s*\(/g;while((r=re.exec(s)))m[r[1]]=(m[r[1]]||0)+1;console.log(Object.entries(m).filter(([,v])=>v>1))"
     ```

   * 配套：CI 的 `verify_build.js` 已含 `node --check` 全量校验，但**本地提交前自己先 check 一次**能省一轮往返。

## 八、🔴 publish\_update.ps1 自愈推送 + 双远程同步（2026-08-31 立规，打包发布必读）

1. **打包期间 CI 自动提交会造成分叉**：`tauri build` 耗时约 3 分钟，期间 CI 会自动 bump `sw.js` 的 `CACHE_VERSION` 并往 `version.json` 注入 `frontVersion`/`deployTag` 字段。这期间本地若已经基于旧 HEAD 准备好了发布 commit，push 必被拒（non-fast-forward）。

   * **事故场景**：本地 release commit 已就绪 → 打包期间 CI 提交了缓存版本号 → 本地 push 被拒 → 误判为"网络问题"反复重试无效。

   * **正确动作**：push 被拒时不要手动 rebase，直接依赖 `publish_update.ps1` 的自愈链路（见第 19 条），不要绕过它用裸 `git push`。

2. **publish\_update.ps1 自愈链路（2026-08-31 commit 93822c0 已内置）**——push 被拒时自动执行：

   1. `git stash` 暂存本地未提交改动（保护工作区）
   2. `git pull --rebase origin main` 同步远端
   3. **version.json 冲突智能合并**（关键）：

      * 发布字段（`version`/`pub_date`/`download`/`signature`/`size`）→ 取本次发布值

      * CI 注入字段（`frontVersion`/`deployTag`）→ 保留 CI 注入值，**不覆盖**
   4. `git rebase --continue` → 重推一次

   * **红线**：version.json 的智能合并规则不许简化成"全用本次发布值"——frontVersion/deployTag 是 CI 部署的部署时钟标记，覆盖了会让前端版本号显示回退。

3. **双远程同步陷阱（最高频踩坑点）**：

   * 本地 `pull --rebase` 会**重写本地 commit 哈希**，Gitee 上的旧哈希从此不在本地历史 → 后续**每次** `git push gitee` 都被拒。

   * **一次性强制同步**：`git push --force-with-lease gitee main`（用户已确认该操作模式，只在双远程同步场景用，日常 push 不许加 force）。

   * **禁止**：`git push --force gitee main`（无 lease 保护，可能误覆盖他人提交）。

4. **Gitee 代码树不同步**：`publish_update.ps1` 只 push GitHub（触发 Pages 部署），不自动 push Gitee。因此 `gitee.com/.../raw/main/updater.json` 会长期停留在旧版本，**绝对不能**用作 updater endpoints。若将来要让索引走 Gitee，必须先改脚本同时 push Gitee，再换 endpoints。

## 九、🔴 波数监控 per-window 独立配置（2026-08-31 commit 0b22ec0 立规）

1. **波数监控配置结构**：

   * 前端配置键：`tfjl_game_monitor_cfg`

   * 结构：`{ winConfigs: { "<窗口标题>": { region, speak, speak_prefix, speak_suffix, key_waves } }, interval_sec }`

   * Rust 侧：`GmWin` 含 region/speak/speak\_prefix/speak\_suffix/key\_waves；`GmCfg` 仅 windows + interval\_sec。

   * **红线**：每个窗口的配置必须独立，禁止退回全局单一 region/speak 的旧结构。
2. **旧全局格式自动迁移**：升级到 2.0.26+ 后，旧的 `tfjl_game_monitor_cfg` 全局格式（region/speak 直接在顶层）会自动平移到每个已勾选窗口的 winConfigs 里。迁移逻辑只跑一次，不许在迁移完成后还保留旧全局字段造成双源混淆。
3. **波数识别必须只截小区域**：禁止整窗截图 + 前端 canvas 转码（体积大、IO 重、GC 压力大）。每拍仅截波数小区域（几十 KB）直传 OCR。监控线程在 Rust 侧独立运行，关窗/最小化仍可播报。
4. **多窗口播报前缀**：多窗口同时监控时，播报必须带「N号窗」前缀区分，否则用户分不清是哪个窗口的波数。

## 十、🔴 升级回退链三级化（2026-08-31 commit 921f0d9 + 0b22ec0 立规）

1. **升级入口统一**：顶部提示 / 底部徽标 / 菜单 三条入口升级逻辑完全相同——先走原生静默升级，失败才回退。禁止给某条入口加"特殊升级路径"造成行为分叉。
2. **升级回退链（v2.0.26 起，三级）**：

   1. `start_installer_silent`（传 `/S /R` 参数，装完自动重启、零双开风险）——**新版本才有**
   2. 写 .bat 文件（带 `installer.exe /S /R`）+ `open_url` 执行——**老包兜底**
   3. `start_umi_ocr` 兜底——**最终兜底，可能弹 NSIS 向导**

   * **红线**：三级链路顺序不许调换，`start_umi_ocr` 必须在最后（它不保证静默）。
3. **升级超时**：原生下载超时从 25 秒延长至 **150 秒**（慢网络误判失败会触发回退弹窗）。禁止改回 25 秒。
4. **升级前自动保存**：升级触发前必须自动保存编辑中的项目数据，防止安装器强杀进程导致数据丢失。
5. **手动双击 setup.exe 弹 NSIS 向导属正常**：APP 内升级走自动更新通道带 `/S` 静默参数后台安装；用户手动双击安装包会弹 NSIS 向导不会带静默参数。这不是 bug，不要"修复"它。

## 十一、🔴 读取线上文件与查 CI 状态（2026-08-31 立规，发布验证必读）

1. **读取 GitHub Pages 线上文件必须带** **`?nocache=<时间戳>`** **参数**：

   ```powershell
   curl.exe -s "https://gyq-svip.github.io/tfjl-web/version.json?nocache=$(Get-Date -UFormat %s)"
   ```

   * **事故**：不带参数会读到 CDN/SW 缓存的旧版本，误判"未部署"，反复触发不必要的二次发布。

   * **适用范围**：updater.json / version.json / index.html / 任意 app-\*.js 的线上内容核对。
2. **GitHub REST API 本机直连或走代理均 403 限流**：查 CI 部署状态不要用 REST API，改用"缓存绕过法"——直接 curl 线上产物（带 nocache）看内容是否更新到位。
3. **curl 校验导出是否齐全的标准做法**（必用 `curl.exe` 不是 `curl`）：

   ```powershell
   curl.exe -s "https://gyq-svip.github.io/tfjl-web/app-local2.js" -o _tmp.js
   Select-String -Path _tmp.js -Pattern "window.openAppLocalSettings|window.runDiagnostics|var isTauriApp"
   ```

## 十二、🔴 版本号同步与 commit 规范（2026-08-31 立规）

1. **Cargo.toml 与 tauri.conf.json 版本必须同步 bump**：`CARGO_PKG_VERSION → __APP_VERSION` 由 Cargo.toml 注入。曾漂移 5 个版本（Cargo.toml=2.0.20 vs tauri.conf.json=2.0.25），导致 APP 内版本号显示与安装包不一致。

   * **排查**：`node -e "console.log(require('./src-tauri/tauri.conf.json').version)"` 与 `Select-String -Path src-tauri/Cargo.toml -Pattern "^version"` 必须一致。

   * **注意**：Cargo.toml 的 version 字段是 crate 名义版本，不影响安装包号——但既然注入给了 \_\_APP\_VERSION，就必须同步。
2. **tools/ 目录被 gitignore**：测试文件（test-\*.mjs）仅本地不进仓库。禁止把测试文件误 `git add` 到根目录其他位置。
3. **git commit -m 不支持 PowerShell heredoc**：中文 commit message 会炸成乱码报 `字符串缺少终止符`。

   * **正确做法**：把 message Write 到 `.git\COMMIT_MSG_TMP.txt` 后 `git commit -F .git\COMMIT_MSG_TMP.txt`。

   * **或**：commit message 一律用英文（见铁律 B 步骤已有规定）。
4. **test-updater-silent.mjs U6b 断言守卫**：升级链相关测试必须断言三级回退链路的顺序与参数（`/S /R`），改回退链时同步更新该测试，禁止只改实现不改测试。

## 十三、🔴 阵容分享闭环（2026-08-31 commit d94f090 + 80ca00f 立规）

1. **分享短链生成**：分享时把阵容 payload 写进**公开 Gist** 的 `lineup.json`，得 `#lg=<gistId>` 短链（\~80 字符）。禁止写进私有 Gist（接收方读不到）。
2. **二维码懒加载**：`qrcode-generator` 库走 `vendor/` 懒加载，`_lineupEnsureQrLib` 有失败重置可重试。禁止在首屏同步加载该库（拖慢启动）。
3. **接收三条路必须全部可用**：① 扫码直达网页版自动弹导入 ② 打开 `#lg=`·`#lineup=` 链接自动弹导入 ③ 导入弹窗直接粘贴整条链接（OK 按钮双路正则解析）。禁止只保留其中一条。
4. **降级策略**：短链或二维码失败时自动降级为纯阵容码 + 长链。复制链接用**固定 GitHub Pages 域**，禁止用 `location.origin`（APP 内 origin 不是网页域名）。
5. **导入防覆盖**：导入阵容时必须弹"导入到"下拉选目标项目，禁止默认覆盖当前项目。

## 十四、🔴 融合卡推送与切皮（2026-08-31 commit cd828cb 立规）

1. **融合卡推送范围**：`git_push_fusions` 必须一次推送 `fusions.json` + `registry.json` + `skins/融合XX/` 整批。禁止只推 fusions.json（切皮产物会漏，需手动去皮肤 tab 二次推送，易遗漏）。
2. **自愈推送**：`push_with_self_heal`——push 被拒时自动 `pull --rebase --autostash` 重推一次。autostash 保护工作区未提交改动。禁止在自愈逻辑里丢弃工作区改动。
3. **切皮增量模式**：已切过的（registry 有融合名 XX 记录）默认跳过，`force=true` 才强制重切。一键全切的进度条必须区分"新切/跳过/失败"三种状态。
4. **添加后自动切皮**：仅新添加的单张融合卡自动切皮，禁止对已存在的融合卡重复切皮（浪费算力）。
5. **完整融合名优先从数据源取**：右键切换主卡皮肤时，完整融合名（如"死神海妖"）优先从 `myPlacedCards`/`teammatePlacedCards` 数据源获取，防止误判为单卡路径导致副卡层删除。

## 十五、🔴 统一导入弹窗与下拉框交互（2026-08-31 commit 82b8a14 + 27409ca 立规）

1. **导入/保存弹窗两级选择**：分类下拉（默认=当前项目所在分类）+ 项目下拉（只列该分类项目，当前项目默认选中）。禁止回退到旧版 optgroup 分组挤一个下拉的选项体验。
2. **下拉框滚轮切换**：`attachSelectWheel` 通用函数——鼠标停在 select 上滚轮即换选项，跳过空值与 `__` 开头特殊项（避免误触弹窗），边界不循环，change 事件派发后才有联动。禁止给特殊项加滚轮切换。
3. **弹窗 z-index 永远盖顶**：动态 `200000+ topWinZIndex`，禁止固定 10000（会被扫描文件查看浮窗 100000+ 压住）。
4. **空内容防误**：stash 来源用 `hasOwnProperty` 判定（含空串），禁止误回退上次生成器残留的 `_activity`/`_dungeonScriptOutput` 把陈旧内容导入项目。

## 十六、🔴 跨文件调用函数名必须先核实存在（2026-08-31 血案：导入全链路报错）

1. **血案**：`confirmImport` 保存成功回调里凭印象写了 `loadCategoriesFromDB()` / `updateCategorySelector()`——两个函数**从未存在**（真实函数是 `loadCategories()` / `refreshProjectSelectors()`），ReferenceError 被 `.catch` 吞掉后显示「恢复项目失败」，实际项目已保存但刷新/弹窗/自动打开全断。链接导入、短码导入、文件导入、需求墙导入全部中招。
2. **规矩**：跨文件调用函数前，先 `Grep "function 函数名"` 确认定义真实存在（本项目无 TS/IDE 补全，函数名全靠记忆必错）。不确定就用 `typeof fn === 'function'` 守卫（参照 app-features.js 的 `_importPromptNewCategory` 用法）。
3. **分类补录**：导入项目可能自带本地没有的分类，保存后必须 `categories.includes(category)` 检查并 push + `saveCategories()` 落库，否则主界面分类下拉永远选不到该分类。

## 十七、🔴 升级气泡反复弹根因：显示版本 ≠ SW 真实版本 + 彩球缺同版本去重（2026-08-31 立规）

1. **血案**：用户已是最新版 0.592，彩球仍"不定时莫名其妙弹"。
2. **根因**：右下角版本号 `_fillRealVersionTag()`（app-picker.js）用**远端 version.json**（线上最新 592）填充显示 → 用户以为已是最新。但版本判断 `_getLocalLoadedVersion()` 取的是 SW 回报的 `versionTag.dataset.swVersion`（真实 SW 版本，可能停在 591）。两者不一致 = 显示骗了用户。
3. **反复弹闭环**：SW 每 5 分钟 `_pollLatestVersion()`（sw\.js）发现"线上 592 > 本地 SW 591"→ 发 FORCE\_RELOAD；页面 `_verifyNewVersion()`（app-picker.js）核实"远端 592 > 本地 SW 591"也判有新版 → 弹彩球。APP 前台不自动升级（见第九章铁律），用户关彩球而非点升级 → SW 永远停在旧版 → 每 5 分钟又弹一次。
4. **规矩（彩球/通知去重）**：任何"新版本提示"入口（`showSwUpdateBanner`、`notifyNewVersion`）必须有**会话内同一目标版本只弹一次**的去重（`window.__bannerShownVersions[目标版本]` + `window.__tfjlNewVersionTarget`），否则任何周期性轮询（SW 5 分钟 / 心跳 15 分钟 / 流量触发 30 秒节流）都会变成无止境骚扰。刷新/重启后会话重置，可再弹一次提醒（合理）。**核实"已是最新"时务必清掉残留的** **`__pendingUpdate`** **假信号**（app-core.js `_verifyNewVersion` 已做），防心跳反复重弹。
5. **自查 SW 真实版本**（别被显示骗）：控制台执行 `document.getElementById('versionTag').dataset.swVersion`（textContent 可能是远端填充的假最新）。不一致时**双击右下角版本号**走 `forceRefreshLatest()`，把 SW 升到真正最新，从源头消除轮询信号。
6. **本地 SW 版本号获取方式**：SW 的 CACHE\_VERSION 是 sw\.js 内部常量，页面无法直接读 controller 内部变量，**只能靠 SW 主动 postMessage 回报**（`SW_VERSION` 消息，install 时与 `GET_SW_VERSION` 请求时发）。页面已缓存到 `versionTag.dataset.swVersion`，这就是唯一可信的本地 SW 真实版本源。

