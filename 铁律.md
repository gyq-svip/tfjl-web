# 铁律（项目约定，违反即视为 bug）

<p style="color:red;font-weight:bold;font-size:1.4em;">📦📦📦 打包发布标准流程（小白教程 · 每次发版严格照做 · 2026-08-29 立）📦📦📦</p>

> **写在最前面**：自动升级出过 3 次事故（2026-08-28 深夜折腾数小时），全部源于"打包环节没按流程走"。
> **从今往后，发版只准照下面 A→B→C→D 四步做，一步都不能跳、不能改顺序。**

## A. 发版前检查（3 分钟，必做）

在仓库根目录 `d:\tfjl-web` 打开 PowerShell，逐条跑：

**A1. 确认工作区干净**
```powershell
git status --short
```
- 有输出 = 还有未提交改动 → 先提交（见 A2）或确认无误。

**A2. 确认当前版本号**
```powershell
node -e "console.log(require('./src-tauri/tauri.conf.json').version)"
```
记下这个号，下一步要 +1。

**A3. 检查 updater 配置（出过事故，必查）**
```powershell
node -e "const j=require('./src-tauri/tauri.conf.json');const u=j.plugins.updater;console.log('version='+j.version);console.log('endpoints='+JSON.stringify(u.endpoints));console.log('installMode='+JSON.stringify(u.windows))"
```
**必须是这样，否则自动升级必然失效：**
```
endpoints   = ["https://gyq-svip.github.io/tfjl-web/updater.json"]
installMode = {"installMode":"quiet"}
```
- ❌ `endpoints` 里**绝对不许**出现 `{{current_version}}`！
  （历史事故：用了 `.../releases/download/v{{current_version}}/updater.json`，
  每个版本都读到"自己那份"，Tauri 永远判定"无更新"，原生升级形同虚设。）
- ❌ `installMode` 不许是 `passive`（会弹 NSIS 安装窗口）。必须是 `quiet`（完全静默）。

**A4. 检查打包 exe 与签名 key 是否匹配**（脚本会自动查，这里先确认文件在）
```powershell
Get-ChildItem src-tauri\target\release\bundle\nsis\*.exe | Select-Object Name,LastWriteTime
```

## B. bump 版本号（单独 commit，铁律第 6 条）

**版本号必须单独一个 commit，禁止混进功能改动里！**

```powershell
# 把 2.0.19 换成「当前版本 +1」
node -e "const fs=require('fs');const p='src-tauri/tauri.conf.json';let t=fs.readFileSync(p,'utf8');t=t.replace(/\"version\":\s*\"[^\"]+\"/,'\"version\": \"2.0.19\"');fs.writeFileSync(p,t)"
git add -A
git commit -m "chore: bump version to 2.0.19"
```

> ⚠️ PowerShell 里**不要用中文 commit message**（编码会炸成乱码报 `字符串缺少终止符`）。
> commit message 一律用英文。

## C. 打包 + 签名（约 5~10 分钟）

**C1. 打包**
```powershell
npx tauri build
```
等待完成。产物在：
```
src-tauri\target\release\bundle\nsis\塔防精灵助手_<版本>_x64-setup.exe
```

**C2. 把 exe 复制到仓库根目录**（`publish_update.ps1` 从根目录取）
```powershell
Copy-Item "src-tauri\target\release\bundle\nsis\塔防精灵助手_2.0.19_x64-setup.exe" ".\tfjl-assistant_2.0.19_x64-setup.exe" -Force
```

**C3. 签名**
```powershell
.\sign.ps1
```
看到 `Sign success and verified!` 才算成功。
脚本会自动：恢复 `tauri.key` → 跑 `verify_build.js` 校验 → 复制成 ASCII 临时名签名 → 校验 pubkey keynum。

> ❌ 签名失败/keynum 不一致 → **停止**，重跑 `npx tauri build` 后重试，绝不跳过。

## D. 发布（写 updater.json + 传 Gitee + 触发 Pages 部署）

```powershell
.\publish_update.ps1
```
它会依次做：
1. 生成根目录 `updater.json` / `version.json`（版本号取自 `tauri.conf.json`）
2. 上传 exe + updater.json 到 **Gitee 发行版 v<版本>**
3. `git commit` + `git push origin main`（触发 GitHub Pages 部署 + 更新线上 updater.json）

看到 `Published: v<版本>` 即成功。脚本最后会自动验证 Gitee 下载直链（HTTP 200 + 大小一致）。

> ⚠️ **Gitee 代码树不会自动同步**（脚本只 push GitHub）。
> 所以 `gitee.com/.../raw/main/updater.json` 会长期停留在旧版本，**不能**用作 updater endpoints。
> 若将来要让索引也走 Gitee，必须先改脚本同时 push Gitee，再换 endpoints。

## E. 发版后验证（5 分钟，必做）

**E1. 确认线上索引已更新**
```powershell
curl.exe -s "https://gyq-svip.github.io/tfjl-web/updater.json"
```
`version` 字段必须 = 刚发的版本号。

**E2. 确认 GitHub Pages 部署完成**
```powershell
curl.exe -s "https://gyq-svip.github.io/tfjl-web/index.html" | Select-String "app-features.js"
```

**E3. 用老版本实测升级**
装一个**比你刚发的版本旧**的包（例如发的是 2.0.19，就装 2.0.16），然后：
- 打开 → 右下角出现 `🔔 新版本 v2.0.19`
- 点它 → 应出现 `⏳ 正在下载更新包…` → `🚀 正在启动安装程序…`
- 安装程序启动 → 完成 → App 自动重开成新版本

**E4. 调试日志核对**
在 App 调试窗跑 `checkAppLocal()`，确认：
```
openAppLocalSettings: "function"
btnDisplay: "flex"
```

## 常见故障速查

| 现象 | 原因 | 解决 |
|---|---|---|
| 点升级没反应 / 永远"无更新" | endpoints 含 `{{current_version}}` | 按 A3 改成固定 GitHub Pages 地址 |
| 升级弹 NSIS 安装窗口 | `installMode` 是 `passive` | 改成 `quiet`（需重新打包） |
| `schannel: SSL/TLS connection failed` | git 走系统代理握手失败 | 用 `git -c http.sslVerify=false push origin main` |
| `Updates were rejected (fetch first)` | CI 自动 commit 了 sw.js | 先 `git -c http.sslVerify=false pull --rebase origin main` 再 push |
| PowerShell 报"字符串缺少终止符" | 命令里含中文 | commit message / 参数改用英文，或用 `git add -A` 避开中文文件名 |
| 某 JS 文件所有导出都 undefined | 该文件有 TDZ 错误（const 用在前、声明在后） | 见第 13/14 条 |
| 前端改了用户没生效 | Service Worker 缓存 | App 里双击右下角版本号，或调试窗跑 `clearSWCache()` |

---

<p style="color:red;font-weight:bold;font-size:1.2em;">🔴🔴🔴 铁律第一条（最高优先级，违反即视为严重 bug）🔴🔴🔴</p>

<p style="color:red;font-weight:bold;">❗❗❗ 绝对禁止修改、删除、重构、或任何方式动右下角的版本号标记（#versionTag）及其 tooltip、绑定、文案、弹窗相关逻辑！❗❗❗</p>

<p style="color:red;font-weight:bold;">❗ 双击 #versionTag 即可直接更新，原本就能用，不需要任何弹窗、不需要"点击检查更新"、不需要改 tooltip 文案、不需要加 onclick！❗</p>

<p style="color:red;font-weight:bold;">❗ 该标记的渲染、文案、s日期-HHMM · s1.0.xxx 格式全部由 CI 自动注入（见下方说明），任何提交都不许手动改它！❗</p>

<p style="color:red;font-weight:bold;">❗ 之前 AI 擅自把 #versionTag 的 ondblclick 改成 onclick、加"点击检查更新"弹窗、改 tooltip 文案，全部已被 revert 还原。今后谁再动这里，立即 revert！❗</p>

> 本文件为 tfjl-web 强约束。任何涉及 GitHub Gist / Token / 统计读写 / 自动发布的改动，必须先读本文件。
> 部署方式：`deploy.yml` 监听 `push: [main]`（及 `workflow_dispatch`），push 到 main 即自动部署到 GitHub Pages；token 由该 workflow 的 "Inject Token" 步骤注入。
> 每次部署 CI **自动**做两件事（无需手改源码）：① bump 所有脚本 `?v=` 缓存版本号（cache-bust，防 Service Worker 跑旧 JS）；② 把页面右下角 `#versionTag` 注入本次部署时间标记，格式 `s日期-HHMM · s1.0.xxx`（如 `s20260824-2033 · s1.0.230`）。**前半段 `s日期-HHMM` = 发布/部署时钟时间（即你要的"发布时间"），每次部署都变，凭此判断最新；后半段 `s1.0.xxx` = `sw.js` 的 `CACHE_VERSION` 语义版本号（如 230），是缓存大版本、不是部署次数，禁止改成部署序号**。**判断"是否最新版"以线上页面右下角实时标记为准，不是看本地 `index.html` 的静态值**。

## 一、Gist 所有权与读写规则（最高优先级）

1. **所有 Gist 都属于 `gyq-svip` 这个账号自己创建的**（用户明确确认：所有 Gist 都是 gyq-svip 账号手动/自动创建的，不存在"别人的 Gist"）。
   - 所有 Gist ID 在代码中**硬编码**，禁止代码自动发现/自动创建陌生 Gist。
   - 计数器 Gist：`COUNTER_GIST_ID = 'e1bd9a5139e1c4e011bfea707e917d61'`（归 gyq-svip）
   - 索引 / 消息 / 广播等 Gist：同理，ID 必须硬编码或来自你已知清单，不得运行时凭空生成。
2. **禁止"自动创建新 Gist 并缓存到 localStorage 然后长期使用"**。代码里凡是
   `localStorage.setItem('counter_gist_id', responseData.id)` 或从 `index['counter']` 覆盖 `COUNTER_GIST_ID`
   的逻辑，一律删除——这是"出现非我创建的 Gist"的唯一来源。
3. **读写策略（默认你有权限，不做多余预检，仅对真实响应兜底）**：
   - Token 由部署自动注入（见第 4 条），**代码不要做"先验我有没有权限再放行"的额外关卡**——拿到 token 直接去读写，正常情况一路畅通，不耽误事。
   - 仅当 GitHub 真实返回 **401/403/404** 时才处理：
     - 明确 `console.warn('[Gist] <用途> 读写失败 status=<code>，请检查 Token/权限')`
     - **立即 return / 停止重试**，**绝不允许**转而创建匿名新 Gist 或用别的 Gist 顶替（避免写出归匿名的 Gist 导致永久不可读写）。
   - 说明：401 判断是安全网不是障碍——它能在 token 注入失效时立刻暴露问题（如 2026-08-24 限额变 60 事故），删掉反而会让代码静默用匿名身份写、产生你永远读不回的 Gist。
4. Token 来源：① `app-core.js` 顶部 `HARDCODED_TOKEN = 'YOUR_GITHUB_TOKEN_HERE'` 占位符，**由 `deploy.yml` 的 "Inject Token" 步骤在部署时替换为 `secrets.GIST_TOKEN`**（这是网页版/APP版上线的唯一 token 来源）；② `localStorage['TFJL_Gist_Token']`（本地开发/用户手动填写）；③ iframe 父窗口注入（兜底）。`HARDCODED_TOKEN` 占位符以 `ghp_` 开头且 length>20 才生效，占位符原文 `YOUR_GITHUB_TOKEN_HERE` 不生效。
   - **严禁删除 `HARDCODED_TOKEN` 占位符及其注入逻辑**——2026-08-24 曾因全仓去敏误删该占位符，导致部署后 `getGistToken()` 永远返回空（API 监控 token 全 X、rate_limit 限额变 60、需求墙/分享误报离线）。`deploy.yml` 第 84-87 行仍在按 `YOUR_GITHUB_TOKEN_HERE` 做 sed 替换，源文件占位符缺失会导致注入静默失败。
   - 用户明确确认：**"我的 token 是自动注入的，占位符不能删除的"**——这是硬约束。今后任何去敏 / 重构 / "死代码清理"都**绝不可删** `HARDCODED_TOKEN = 'YOUR_GITHUB_TOKEN_HERE'` 及 `getGistToken` 里的占位符读取分支。判断某段像死代码前，必须先确认是否有外部注入/构建脚本（deploy.yml 等）依赖它。
   - Token 缺失/失效时相关写操作**静默跳过 + 明确 WARN**，不得抛未捕获异常、不得崩溃刷屏。

## 二、版本号与发布

5. `version.json` 的 `version` 必须与 `src-tauri/tauri.conf.json` 的 `version` 一致，由 `publish_update.ps1` 统一写入，禁止手改两处不同步。
6. 版本 bump 必须**单独 commit + push**（铁律发布流程），禁止把版本号混进功能提交。

## 三、Tauri 命令授权（权限铁律）

7. 任何新增/重命名 Tauri 命令，必须**三处同步**：`src-tauri/src/lib.rs`（invoke_handler）、
   `src-tauri/capabilities/allow-custom-commands.toml`（自定义权限 identifier，如 `allow-read-text-file`）、
   `src-tauri/capabilities/default.json`（capability 引用 identifier）。三处缺一则打包失败。
   - 注意：capability 引用的是 toml 里 `[[permission]]` 的 `identifier` 字段，**不是** Rust 命令名。
8. 每次打包前必须跑 `.github/verify_build.js` 全量校验（含"23 命令三处一致"），不通过不得打包。

## 四、网页与 APP 一致性

9. 任何影响网页端的功能改动，必须同步 `webroot/` 同名文件（APP 内嵌用 webroot），根与 webroot 双份提交。
10. 下载桌面版弹窗的"软件大小"动态读取 `version.json.size` 字段，由 `publish_update.ps1` 自动写，
    不要手写死（无字段时回退静态文案）。

## 五、错误处理

11. 任何 `try/catch` 外的变量必须 `let/const` 声明；禁止隐式全局（曾因 `content` 未声明导致
    `ReferenceError: content is not defined` 掩盖真实 401）。
12. 调试日志面板（天使日志）必须全量收集 `console.*` + `window.onerror` + `unhandledrejection`，
    与 F12 Console 一致。浮窗开关只存在于调试日志面板内，不另占管理员菜单。

## 六、🔴 const/let 声明顺序（TDZ）——新增功能插常量时必查（2026-08-28 血案）

13. **`const` / `let` 必须先声明、后使用。往文件里新插常量前，必须确认下方更早位置是否已在引用它。**
    - 违反会抛 `ReferenceError: Cannot access 'XXX' before initialization`（TDZ 暂时性死区），
      **且该错误会让整个 JS 文件静默中断**，表现为"该文件所有 `window.*` 导出全部失效"。
    - **2026-08-28 事故**：`app-local2.js` 第 529 行 `settingsConfig = { ... autoBackupKeep: AUTO_BACKUP_DEFAULT_KEEP ... }`
      使用了常量，但 `const AUTO_BACKUP_DEFAULT_KEEP = 20` 声明在 **543 行（后面 14 行）**。
      → 整文件中断 → 4307 行 `window.openAppLocalSettings = ...`、`window.scanSkins` 等全部没挂上
      → App 里"APP设置"按钮 `display:none`、点击报 `openAppLocalSettings is not a function`。
      根源是当天新增"自动增量备份"功能时，把 `AUTO_BACKUP_*` 整批常量插到了 `settingsConfig` **之后**。
    - 修复：常量声明整批前移到 `settingsConfig` 之前（根 + `webroot/` 的 `app-local.js`、`app-local2.js` 四份同改）。
14. **这类错误的危害在于"症状完全不像声明问题"**：只表现为"功能失效/导出没了/按钮不显示"，
    控制台仅一闪而过一行 ReferenceError，极易被误判成 **Service Worker 缓存 / 部署不对 / 文件残缺**。
    本次就因此白排查数小时（反复 clearSWCache、重新打包 2.0.18、卸载重装，全部无效——
    因为 `curl` 线上文件一直是好的，导出齐全，根本不是缓存问题）。
    **凡遇"某 JS 文件的导出全部 undefined"，第一嫌疑就是该文件头部有 TDZ / 运行时错误，先查代码顺序，别急着怪缓存。**

## 七、Tauri App 前端报错排查手法（2026-08-28 确立）

15. **Tauri App 内 F12 打不开 DevTools；浮动调试窗的 `floatConsoleExec` 对长命令会被 eval 截断**
    （含 `.then()` 链的长句常报 `SyntaxError: Unexpected token ')'`，命令根本没执行、日志里连记录都没有）。
    → 排查 App 前端执行错误时，**改用 `playwright-cli` 开干净浏览器直接打线上 URL**：
    ```bash
    npm i -g @playwright/cli@latest
    playwright-cli open "https://gyq-svip.github.io/tfjl-web/?t=<随机>"
    playwright-cli console          # 读完整堆栈（本次就是靠它抓到 app-local2.js:529:122）
    playwright-cli eval "JSON.stringify({oa: typeof window.openAppLocalSettings})"
    playwright-cli dialog-accept    # 网页版点设置会弹「未找到Tauri invoke」提示，属正常
    playwright-cli close
    ```
    - **网页版也能验证 App 专属脚本的导出**：`app-local2.js` 的导出在 `if(true)` 块内无条件执行，
      浏览器里同样会挂上 `window.openAppLocalSettings`，不必非装 App 才能测。
    - 注意：`playwright-cli` 默认**不能**访问 `file://`（报 protocol blocked）；
      起本地 `http-server` 又常起不来时，直接部署后用浏览器打线上域名最省事。
16. `curl` 校验线上文件要用 **`curl.exe`**（PowerShell 里裸 `curl` 是 `Invoke-WebRequest` 别名，参数不兼容会报错）。
    核对导出是否齐全的标准做法：
    ```powershell
    curl.exe -s "https://gyq-svip.github.io/tfjl-web/app-local2.js" -o _tmp.js
    Select-String -Path _tmp.js -Pattern "window.openAppLocalSettings|window.runDiagnostics|var isTauriApp"
    ```
    推 GitHub 遇到 `schannel: SSL/TLS connection failed` 时，用 `git -c http.sslVerify=false push origin main`
    （保留本机 7897 代理，别用 `publish_update.ps1` 里的 `-c http.proxy=` 清空写法，那会直接超时）。
