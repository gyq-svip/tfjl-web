# 塔防精灵助手 · 管理员使用说明

> 面向维护「卡牌数据 / 皮肤资源」的管理员。所有数据都从 GitHub Pages 实时拉取，改完推 Pages 即生效，不用重打包 exe。

---

## 0. 管理员进门
- 入口：主界面「管理员面板」（需输入管理员验证码；验证码由本地 PBKDF2 哈希校验，无需联网）。
- 进门后可看到「🃏 卡组管理」等入口。

---

## 1. 添加融合卡（有图形界面，推荐）
入口：**管理员面板 → 🃏 卡组管理 → 「添加融合卡」页签**。

- 选职业、选品质（金/紫）、填融合卡名（**必须正好 = 100 张基础卡里的 2 张**，例如「火炮射线」= 火炮 + 射线）。
- 系统校验通过后写入本地 `skins/fusions.json`，并提示推送。
- 下方「📋 已添加的融合卡」区可查看全部已加融合卡（组成 + 职业/品质标签），每条带「🗑 删除」。
- 推送：点「🚀 一键推送 fusions.json 到 GitHub」（桌面端，经 Rust 命令自动 `git add/commit/push`）；或手动 `git push origin main`。

融合卡本质 = 2 张基础卡的合成显示，属性沿用第一张，**只显示皮肤**，不进皮肤选择器 / 脚本生成。

---

## 2. 添加「新基础卡」（新英雄，云端 cards.json）
> 游戏以后出新英雄（不在原 100 张里），**不用再改代码、不用发版**。

- **文件**：`skins/cards.json`（仓库内，随 GitHub Pages 自动部署）。
- **结构**：
```json
{
  "cards": {
    "新英雄名": { "quality": "金", "profession": "战士" }
  }
}
```
  - `quality`：`金` 或 `紫` —— 决定进「金卡类」/「紫卡类」。
  - `profession`：`工程` / `战士` / `法师` / `射手` / `召唤` / `牧师` / `术士` / `熊猫` / `精灵球` —— 决定进哪个职业子分类。
- **操作**：用编辑器打开 `skins/cards.json`，在 `cards` 对象里加一行即可。仓库初始模板是 `{ "cards": {} }`（空，可照格式填）。
- **生效**：保存 → `git push origin main` → 用户 App 重启 + 硬刷新，新英雄自动出现在对应金/紫卡类的职业子分类下（与基础卡、融合卡并列，点 / 拖即可上阵）。
- ⚠️ 当前该入口是「**直接编辑 json 文件**」（暂无独立图形按钮）。因为它比融合卡简单——只需名字 + 职业，不涉及皮肤切图。若需要图形入口可后续补。

> 新英雄会被系统识别为「基础卡」：融合卡校验（"正好是基础卡里的 2 张"）、卡组管理英雄下拉、英雄名集合都包含它，所以新英雄也能当作融合卡的基础卡来用。

---

## 3. 上传皮肤立绘
> 只加卡名 + 职业，新英雄上阵只有职业渐变底色；要带皮图需另传皮肤 png。

- **位置**：`skins/{英雄名}/{皮肤名}.png`（每个英雄一个文件夹，例如 `skins/小野/`）。
- **命名**：默认皮肤建议 `{英雄名}.png`；其它皮肤任意名，如 `小野_皮肤A.png`。
- **登记**：在 `skins/registry.json` 的 `heroes` 下为该英雄追加条目：
```json
"新英雄名": [
  { "name": "新英雄名", "file": "新英雄名.png" },
  { "name": "皮肤A", "file": "新英雄名_皮肤A.png" }
]
```
  - `name` = 皮肤在下拉里显示的称呼；`file` = 文件名（**不含文件夹前缀**）。
- **上传云端**：把 `skins/{英雄名}/` 文件夹 + 改好的 `registry.json` 一起 `git push origin main`（Pages 自动部署）。
  - 注意：融合卡面板的「一键推送」按钮**只推 fusions.json**，不含皮肤 png / registry.json / cards.json，这部分需手动 `git push`。
- App 运行时从 `https://gyq-svip.github.io/tfjl-web/skins/...` 实时拉取皮肤。

---

## 4. 云端同步机制（给用户的交代）
- 所有卡 / 皮肤数据都从 **GitHub Pages**（`gyq-svip.github.io/tfjl-web`）实时拉取，前端改完推 Pages 即生效，**不用重打包 exe**。
- 推送命令（仓库根目录执行）：
  - GitHub：`git push origin main`（走代理）
  - Gitee：`git -c http.proxy= -c https.proxy= push gitee`（直连）
- 用户侧：已开 App 需 **重启 + 硬刷新**（点右下角版本标记）才能拉到最新数据。

---

## 5. 速查
| 想做 | 入口 / 文件 | 推送方式 |
|------|------------|---------|
| 加融合卡 | 管理员面板 → 卡组管理 → 添加融合卡 | 一键推送按钮 / `git push origin main` |
| 加新基础卡（新英雄） | 编辑 `skins/cards.json` | `git push origin main` |
| 加皮肤立绘 | `skins/{英雄名}/*.png` + 改 `skins/registry.json` | `git push origin main` |
| 删融合卡 | 卡组管理 → 已添加列表 → 🗑 | 一键推送按钮 / `git push` |
